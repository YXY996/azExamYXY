# Project context

Read `PROJECT_MEMORY.md` before starting project work. Follow the shared decisions and update the memory after durable decisions or completed milestones.

Do not store credentials, private PDF contents, or question answers in project memory. Do not modify anything under `sources/`.

Note: the configured OmniRoute launcher currently starts Claude Code with `--bare`, so this file may not be discovered automatically. The caller must explicitly request reading `PROJECT_MEMORY.md`.

